python-telegram-bot>=21.4,<22
httpx>=0.27,<0.29
beautifulsoup4>=4.12,<5
# Only needed when running inside Colab / Jupyter (already patched in Colab
# kernels >= 2024, but harmless elsewhere).
nest_asyncio>=1.6
